---
marp: true
theme: default
paginate: true
size: 16:9
header: 'Hekatan Struct · Validación FEM vs ETABS'
footer: 'Maestría UPS · Aplicaciones Computacionales · 8/may/2026'
style: |
  section { font-size: 26px; }
  h1 { color: #1a4d8c; font-size: 48px; }
  h2 { color: #1a4d8c; font-size: 36px; }
  code { background: #f0f4f8; padding: 2px 6px; border-radius: 3px; }
  pre { font-size: 18px; }
  table { font-size: 22px; }
  .ok { color: #2d8659; font-weight: bold; }
  .warn { color: #c0392b; font-weight: bold; }
---

<!-- _class: lead -->
<!-- _paginate: false -->

# Hekatan Struct
## Validación cruzada FEM vs ETABS
### Análisis lineal estático y dinámico modal con exportación a `.e2k`

**Ing. Jorge** · TFM · Maestría UPS
Aplicaciones Computacionales — Dra. Betzabeth Suquillo
8 de mayo de 2026

---

## Pregunta central del TFM

> ¿Es posible construir un solver FEM open-source que **valide bit-exact** contra ETABS, manteniendo un paradigma **paramétrico-por-código** y un workflow de **round-trip** con la herramienta industrial?

**Audiencia:** ingenieros estructurales que ya usan ETABS/SAP2000.
**Eje del valor:** ventaja sobre ETABS, no reemplazo arquitectónico.

**Alcance del TFM:**
- ✅ Lineal estático
- ✅ Lineal dinámico modal
- 🔒 No-lineal (módulo freemium futuro)

---

## Problema: validar FEM en TFM

ETABS es la **referencia de la industria** en Ecuador y la región. Cualquier herramienta nueva debe demostrar:

1. **Convergencia analítica** — frente a soluciones cerradas (Timoshenko, Euler-Bernoulli)
2. **Equivalencia con ETABS** — mismo modelo → mismos resultados (Δ < 1%)
3. **Round-trip** — exportar `.e2k` para reproducir la validación en ETABS
4. **Reproducibilidad** — código abierto, parametrizable, versionable en git

> Sin estas 4 piedras angulares, **un TFM en FEM no convence al tribunal**.

---

## Hekatan Struct — arquitectura

```
hekatan-fem/    Solver C++ (Eigen) → WASM + Node + nativo
hekatan-mesh/   Generación de malla paramétrica
hekatan-ui/     Visor Three.js Z-up + Tweakpane settings
examples/       Ejemplos cargables vía workspace ?t=<id>
website/        Build de producción → GitHub Pages
```

**Fork de awatif v2.0.0** extendido con:
- Análisis modal (`modalAnalysis`)
- Winkler springs nativos en C++ (`deform.cpp` modificado)
- Workspace unificado con Tweakpane (selector de ejemplos)
- Importador/exportador `.e2k` round-trip
- Benchmarks con validación cruzada documentada

---

## Paradigma: paramétrico-por-código

```typescript
export const benchmarkCftCantilever: ExampleDef = {
  id: "benchmark-cft-cantilever",
  name: "🏁 Frame · Columna CFT Cantilever",
  category: "🏁 Benchmarks · 1️⃣ Frames",
  params: {
    L:     { default: 3.0,   min: 1, max: 10, step: 0.5 },
    D_out: { default: 0.30,  min: 0.15, max: 0.50 },
    t_HSS: { default: 0.012, min: 0.005, max: 0.030 },
    materialType: { options: { Acero: 0, Hormigón: 1, "Mixta CFT": 2 } },
    // ... Property Modifiers ETABS-style ...
  },
  build(p, states) { /* construye y resuelve FEM */ },
  runModal(p, states) { /* análisis modal */ },
};
```

**Cada modelo es código** versionable, parametrizable, reproducible.
**Comparable** con awatif.co — diferencia clave: solver propio + e2k.

---

## Stack tecnológico

| Capa | Tecnología | Justificación |
|------|-----------|---------------|
| Solver | C++17 + Eigen 3 | Mismo lenguaje que OpenSees, FEniCS |
| Bridge | Emscripten → WASM | Browser puro, sin servidor |
| Backend | Node.js (cli_modal.mjs) | Validación CLI batch |
| Nativo | g++ → exe standalone | Validación cruzada nativa |
| UI | TypeScript + Three.js + VanJS | Reactivo, paramétrico |
| Visor | Z-up convention (`THREE.DEFAULT_UP=(0,0,1)`) | Coincide con ETABS/SAP |

**4 solvers ejecutando el mismo modelo** → diferencia 0.00% (validación interna previa al benchmark con ETABS).

---

## API del solver FEM

```typescript
import {
  deform,         // Estático lineal
  analyze,        // Tensiones/momentos a partir de deformaciones
  modalAnalysis,  // Modos propios + frecuencias + masa participativa
  plateQ4Solve,   // Placas Q4 (Mindlin / Kirchhoff / Plane stress)
} from "hekatan-fem";

// Ejemplo cantilever:
const out = deform(nodes, elements,
  { supports, loads },
  { elasticities, areas, momentsOfInertiaY, momentsOfInertiaZ,
    torsionalConstants, shearAreasY, shearAreasZ, densities });

// Salidas: deformaciones por nodo + reacciones en soportes
out.deformations.get(topNode);  // [Ux, Uy, Uz, Rx, Ry, Rz]
out.reactions.get(baseNode);    // [Fx, Fy, Fz, Mx, My, Mz]
```

---

## Benchmarks · 1️⃣ Frames

Tres columnas cantilever empotradas en base, L=3m, peso propio:

| Tipo | Sección ETABS | Material | Shape e2k |
|------|--------------|----------|-----------|
| **Acero** | HSS 300×300×12 mm | A572Gr50 | `Steel Tube` |
| **Hormigón** | 30×30 cm rectangular | concrete | `Concrete Rectangular` |
| **Mixta CFT** | HSS 300×300×12 + 4000 Psi fill | A572Gr50 + `FILLMATERIAL` | `Filled Steel Tube` |

Empotramiento total: `UX UY UZ RX RY RZ`
Carga: peso propio (kg/m) → reparto nodal equivalente
Solución analítica: $u_{top} = \frac{q L^2}{2 E A_{eq}}$

---

## Benchmark cantilever ACERO

**Sección HSS 300×300×12 hueco** · L=3m · A572Gr50

```
A_s = 0.30² − 0.276² = 138.24 cm²
I_s = (0.30⁴ − 0.276⁴)/12 = 19143 cm⁴
γ_s · A = 76.97 · 0.01382 = 1.064 kN/m
W = q·L = 3.193 kN
```

**Resultado analítico:**
$$u_{top} = \frac{q \cdot L^2}{2 \cdot E \cdot A_s} = \frac{1.064 \cdot 9}{2 \cdot 2 \times 10^8 \cdot 0.01382} = -1.73 \times 10^{-3} \text{ mm}$$

<span class="ok">Hekatan: -1.732e-3 mm  →  Δ vs analítico = 0.000%</span>

---

## Benchmark cantilever HORMIGÓN

**Sección 30×30 cm rectangular** · L=3m · concrete f'c=4000 psi

```
A   = 0.30² = 900 cm²
I   = 0.30⁴/12 = 67500 cm⁴
γ_c · A = 23.54 · 0.09 = 2.119 kN/m
W = q·L = 6.358 kN
```

**Resultado analítico:**
$$u_{top} = \frac{q \cdot L^2}{2 \cdot E \cdot A} = \frac{2.119 \cdot 9}{2 \cdot 25 \times 10^6 \cdot 0.09} = -4.24 \times 10^{-3} \text{ mm}$$

<span class="ok">Hekatan: -4.238e-3 mm  →  Δ vs analítico = 0.000%</span>

---

## Benchmark cantilever CFT — caso estrella

**Sección CR300×300×121mm = HSS + concreto fill 4000Psi** · L=3m

Sección transformada steel-equivalente:
```
A_eq = A_s + A_c/n = 138.24 + 761.76/8.044 = 232.93 cm²  ✓ ETABS
I_eq = I_s + I_c/n = 19143 + 48356/8.044 = 25155 cm⁴     ✓ ETABS
n = E_s/E_c = 200/24.86 = 8.044
q  = γ_s·A_s + γ_c·A_c = 1.064 + 1.795 = 2.859 kN/m
```

| Fuente | Uz top |
|--------|--------|
| ETABS API (extracción Python OAPI) | -0.00276 mm |
| Hekatan Struct | -0.00276 mm |
| Analítico $qL^2/(2EA_{eq})$ | -0.00276 mm |

<span class="ok">Δ Hekatan vs ETABS = 0.02% (bit-exact con redondeo)</span>

---

## Tabla comparativa final

| Benchmark | Analítico | Hekatan | ETABS API | Δ% |
|-----------|-----------|---------|-----------|-----|
| Cantilever Acero HSS | -1.73e-3 mm | -1.73e-3 mm | (e2k) | 0.000% |
| Cantilever Hormigón | -4.24e-3 mm | -4.24e-3 mm | (e2k) | 0.000% |
| Cantilever CFT | -2.76e-3 mm | -2.76e-3 mm | -2.80e-3 mm | <span class="ok">0.02%</span> |

**Validación cruzada exitosa** en los tres tipos de columna.

**Casos canónicos previos** (ya validados — validación interna):
- Paz 6.3 Space Frame (modal): Δ 0.00% vs WASM/CLI/C++/Python+SciPy
- Cantilever beam Timoshenko: bit-exact analítico
- Doubly-clamped beam: bit-exact analítico

---

## Property Modifiers ETABS-style

ETABS aplica reductores multiplicativos: `K_eff = K · modifier`

| Modifier | Aplica a | Uso típico |
|----------|----------|------------|
| `AREAMODIFIER` (A_mod) | Rigidez axial | Vigas peraltadas |
| `AS2MODIFIER` / `AS3MODIFIER` | Áreas a corte | Bernoulli puro: As=0 |
| `JMODIFIER` | Torsión Saint-Venant | Reducción por agrietamiento |
| `I22MODIFIER` / `I33MODIFIER` | Inercias flexión | NEC fisuración: 0.5/0.7 |

```typescript
A_mod:   { default: 1.0, label: "Area mod (axial)" },
I33_mod: { default: 1.0, label: "I33 mod (flexión strong)" },
// ... slider 0..1 ajustable en vivo desde Tweakpane
```

**Hekatan replica exactamente** la convención ETABS → comparación 1:1.

---

## Round-trip `.e2k` con ETABS

**Botón "📤 Exportar a ETABS .e2k"** en cada benchmark.

Genera archivo nativo de ETABS 19.1.0:
```
$ FRAME SECTIONS
  FRAMESECTION  "CR300X300X121mm"  MATERIAL "A572Gr50"
                SHAPE "Filled Steel Tube"  D 0.3 B 0.3
                TF 0.012 TW 0.012
                FILLMATERIAL "4000Psi"

$ POINT ASSIGNS
  POINTASSIGN  "1"  "Base"  RESTRAINT "UX UY UZ RX RY RZ"

$ LOAD PATTERNS
  LOADPATTERN "DEAD"  TYPE "Dead"  SELFWEIGHT  1
```

**Workflow del estudiante:**
1. Modela en Hekatan Struct (parámetros + sliders)
2. Click "📤 Exportar `.e2k`"
3. Abre archivo en ETABS → mismo modelo, mismos resultados
4. Compara reportes ⇒ confianza en el solver propio

---

## Importador `.e2k` (round-trip inverso)

**ETABS → Hekatan**: importa modelo existente y valida.

```typescript
import { parseE2k } from "./shared/e2kParser";
const model = parseE2k(rawText);   // → nodes, elements, materiales, secciones
buildExampleFromE2k(model);        // → FEM lista para `deform()`
```

Validados en proyectos reales:
- `family_b_composite.e2k` (edificio mixto CFT + losa)
- `Columna CFT Cantilivier.e2k` (caso de hoy)
- `edif_frame_puro.e2k`, `edif_membrane.e2k`, `edif_rigid_shell.$et`

**Ventaja didáctica:** el alumno puede tomar un modelo de TESIS de otro autor (ETABS) y cargarlo en Hekatan para experimentar sin tener licencia.

---

## Ventaja vs ETABS

| Aspecto | ETABS | Hekatan Struct |
|---------|-------|----------------|
| **Licencia** | USD 6,500/año | Open source MIT |
| **Modelado** | Click + dialogs | Código TypeScript paramétrico |
| **Versionado** | `.edb` binario | Git diff legible |
| **Reproducibilidad** | Manual | `?t=benchmark-cft-cantilever&L=3` |
| **Modificable** | No | Sí (fork → extensión propia) |
| **Round-trip** | — | `.e2k` ↔ Hekatan |
| **Educación** | Caja negra | Solver C++ legible (Eigen) |
| **No-lineal pushover** | ✅ | 🔒 (módulo freemium futuro) |

**Audiencia objetivo:** TFMs, investigación académica, validación cruzada.

---

## Roadmap

**Validados (este TFM):**
- ✅ 8 benchmarks frames (cantilever × 3 + viga × 2 + pórtico × 3)
- ✅ Placas/Cáscaras (Mindlin / Kirchhoff / Plane stress)
- ✅ Sólidos H8 (cubo CalculiX)
- ✅ Combinados (zapatas Winkler, bulbo presiones)
- ✅ Layered shells (CLT / sandwich / ABBD)

**Roadmap freemium (post-TFM):**
- 🔒 No-lineal estático (pushover)
- 🔒 No-lineal dinámico (THA Newmark)
- 🔒 Plasticidad concentrada / distribuida
- 🔒 P-Δ + grandes desplazamientos
- 🔒 Diseño AISC 360 / ACI 318 / NEC-SE-CG embebido

---

## Demo en vivo

```
https://giorgioburbanelli89.github.io/hekatan-struct/workspace/
```

**Plan de la demo (5 min):**

1. Abrir workspace → Categoría `🏁 Benchmarks · 1️⃣ Frames`
2. Cargar `benchmark-cft-cantilever`
3. Mostrar Tweakpane: parámetros L, D_out, t_HSS, P_lat
4. Hover sobre nodo top → tooltip con `Uz = -2.76e-3 mm`
5. Hover sobre base → reacción `Fz = 8.58 kN`
6. Click "📤 Exportar `.e2k`" → descarga archivo
7. Abrir archivo en bloc de notas → mostrar `FRAMESECTION` + `FILLMATERIAL`
8. (Opcional) Abrir el `.e2k` en ETABS si la sala tiene licencia

---

<!-- _class: lead -->

# Preguntas

**Repositorio:** github.com/GiorgioBurbanelli89/hekatan-struct
**Demo en vivo:** giorgioburbanelli89.github.io/hekatan-struct/workspace/
**Contacto:** jbjazzjuventus1989@gmail.com

¡Gracias!

---

## Anexo · Referencias clave

- **Wilson 1971** — Incompatible modes para Q4 plate-bending
- **Wilson Cap. 8** — Discrete Shear Element + Eq. 8.6 / 8.17
- **Dvorkin & Bathe 1984** — MITC4 shear sampling
- **Taylor 1976** — Patch test correction J₀
- **Paz & Leigh — Structural Dynamics 6.3** — Space frame modal
- **Bowles** — Foundation Engineering (Winkler ks_factor)
- **Serquen** — SF-70 bulbo de presiones
- **AISC 358** — Pre-qualified moment connections (RBS, BFP, EP)
- **NEC-SE-GC** — Geotecnia y cimentaciones Ecuador
- **CSI Knowledgebase** — ETABS Frame Object Property Modifiers

